# ANGLE A — Identité + reproduction directe de `C:\t17rr_0906` — GEL du baseline (08/09/2026)

> [AMENDEMENT 08/09 suite à B1 — voir `B1_RAPPORT_DECOMPOSITION_08_09_2026.md`] :
> le témoin de sortie A2 (`bc99cc09…`) dépend de contenus bo1..4 **non spécifiés** par xclbin_replay.exe
> (BOs non initialisés par l'outil). Les verdicts state=4 / déterminisme intra-process / 5 BO déclarés,
> arg3 seul écrit : MAINTENUS. Le protocole reproductible officiel de sortie devient :
> **5 BO data = 0xAA → SHA `df78300f…`** (validé inter-process : b1_diag_allaa + b1_decompose_v2).

> Exécution de l'angle A uniquement. Pas de recompilation, pas de modification runtime/kernel.
> Contrat respecté : une seule variable à la fois ; lecture seule ailleurs.
> Tâche bus de rattachement : `TASK_QUEUED_20260906_172225_020` (governor_mcp — diagnose/repro f3best 9B RR).

---

## A1 — Identité de l'artifact

`C:\t17rr_0906\` = arbre de build MLIR-AIE complet du 06/09 (11:15–11:20), 194 fichiers.
Contenu : `.xclbin` (185 375 o), **`.bin` (3 172 o = 793 mots, taille documentée 15/08)**,
`main.pdi`, 8 core ELFs, kernel objet `layer_fused_bcast_kc256_rr.o`, `.mlir` source
`decode_layer_f3best_4096x12288_d256_g32_s128_a2_kv4_mc_preq_vexp_vreg_dq8_qp_mxp_ub_amac_nokv_tb_rr`.
Pas de buffers ni logs : répertoire d'artefacts purs.

### Hashes clés (SHA256)

| Fichier | SHA256 (début) | Rôle |
|---|---|---|
| `t17rr_0906\…rr.bin` | `02580f41…` | **CTRLCODE** — IDENTIQUE au `RUNTIME_FONCTIONNEL_02_09\kernels_s128_fullk\…rr.bin` ✔ |
| `t17rr_0906\…rr.xclbin` | `a2c290fa…` | xclbin frais 06/09 (rebuild ; diffère de `kernels_s128_fullk` `c2f46ae2…` et `kernels_s128_legacy` `2922ba61…`) |
| `kernels_s128_legacy\…rr.bin` (+`.insts` half-K cache) | `339d36bf…` | génération half-K (era 4217824, `C:\t17rr`) — **génération différente** |
| `RUNTIME_FONCTIONNEL_02_09\bin\replay_bo0_out.bin` | `6b5b5c4a…` | témoin de sortie 02/09 (produit par la génération half-K) |
| `repos_soket1\build\bin\xclbin_replay.exe` | `cd0ca6bb…` | **identique** à la copie backup 02/09 → exécutable gelé ✔ |

**Chaîne d'identité établie** : le ctrlcode de `t17rr_0906` est octet-pour-octet celui archivé le 02/09
dans `kernels_s128_fullk` (fix half-K → full-K). Le xclbin est un rebuild fonctionnellement équivalent
(même géométrie, même ctrlcode). L'artifact est bien **f3best 9B s128 a2_kv4 RR full-K**.

---

## A2 — Reproduction directe (0 recompilation)

Commande exacte (rapport 02/09 §3.5, protocole 0xAA) — 3 répétitions, zéro paramètre changé :

```
cd E:\trixdna_test\repos_soket1\build\bin
X=C:\t17rr_0906\decode_layer_f3best_4096x12288_d256_g32_s128_a2_kv4_mc_preq_vexp_vreg_dq8_qp_mxp_ub_amac_nokv_tb_rr
./xclbin_replay.exe "$X.xclbin" "$X.bin" 4194304 4194304 4194304 4194304 4194304 3 3172 3 bo0=aa_4MB.bin
```

- Input `aa_4MB.bin` recréé déterministe : 4 194 304 o de `0xAA`, SHA256 `9f0d861b…`
- 5 BOs (arg3..arg7) 4 Mo chacun ; bo0 chargé depuis fichier ; opcode=3 ; ninstr=3172

### Résultats

| Mesure | Run 1 | Run 2 | Run 3 |
|---|---|---|---|
| state 1er dispatch | **4 COMPLETED** (8502 µs) | **4** (7578 µs) | **4** (7629 µs) |
| **bo0 post-csum** | `578da392cd6c188a` | `578da392cd6c188a` | `578da392cd6c188a` |
| **SHA256 sortie bo0 complète** | `bc99cc09…` | `bc99cc09…` | `bc99cc09…` |
| bo1..bo4 post-csum | = pre-csum (intacts) | = pre-csum | = pre-csum |
| steady µs/dispatch | 4909.7 | 4904.7 | 4905.3 |

- **Sortie bit-exact identique sur les 3 runs** alors que les group_ids (adresses physiques)
  diffèrent à chaque run (`262143/196609…` → `327679/262145…` → `393215/327681…`) :
  la sortie est **indépendante des adresses d'allocation** → déterminisme réel, pas coïncidence.
- Seul **arg3 (bo0)** est écrit ; arg4..arg7 intacts → contrat d'arguments exécuté = connu et stable.
- Timing : ~4,905 ms/dispatch en full-K. Cohérent avec le ~2,5 ms/layer documenté en half-K
  (travail plein-K ≈ 2× colonnes). Le témoin 02/09 (2036 µs, csum `6c24b0d5…`) appartient à la
  génération half-K (`C:\t17rr`) et **n'est pas comparable** — différence expliquée, pas une contradiction.
- Écart de hash sortie vs témoin 02/09 (`6b5b5c4a…` vs `bc99cc09…`) : même cause — le témoin 02/09
  a été produit avec le `.bin` half-K (`339d36bf…`), pas le full-K (`02580f41…`).

---

## Verdicts

| Question | Verdict |
|---|---|
| L'artifact reproduit-il le baseline ? | **PASS** — ctrlcode identique à l'archive 02/09 (`kernels_s128_fullk`) ; xclbin rebuild équivalent |
| state=4 est-il reproductible ? | **PASS** — 3/3, y compris le premier dispatch |
| La sortie est-elle identique ? | **PASS** — bit-exact ×3 intra-outil (SHA256 `bc99cc09…`), indépendante des adresses physiques — [AMENDE 08/09] voir note en tête : contenus bo1..4 non spécifiés ; protocole reproductible = 5 BO 0xAA → `df78300f…` |
| Le contrat d'arguments exécuté est-il celui documenté ? | **PASS** — 5 BOs déclarés, écriture arg3 uniquement, arg4-7 intacts |
| L'artifact est-il réellement f3best/RR ? | **PASS** — nom, .bin 3172 o/793 mots, `layer_fused_bcast_kc256_rr.o`, arbre de build complet |
| Peut-il servir de référence pour B→E ? | **PASS** → GEL ci-dessous |

⚠️ Portée exacte : A2 démontre le **déterminisme du dispatch NPU** sur input protocole 0xAA.
La validation **numérique** bf16 (golden GEMV INT4, RMSNorm) a été faite séparément (02/09, 31/08)
et n'est pas re-démontrée ici. Ne pas surinterpréter.

---

## GEL (témoin expérimental)

**Artifact gelé** : `C:\t17rr_0906\decode_layer_f3best_4096x12288_d256_g32_s128_a2_kv4_mc_preq_vexp_vreg_dq8_qp_mxp_ub_amac_nokv_tb_rr{.xclbin,.bin}`
**Empreinte témoin** : input `9f0d861b…` (0xAA×4 Mo) → sortie `bc99cc09…`, post-csum `578da392cd6c188a`, state=4, ~4 905 µs/dispatch full-K.
**Règle** : on ne touche plus à A. Toute mesure B/C/D/E se fait PAR RAPPORT à ce témoin.

## Suite

- **B** (goulot) mesurera séparément : host prep → BO/staging → soumission XRT → exécution NPU → DMA/DDR → sync → layer suivant. Le mur DDR 19–23 GB/s cadre le budget, il ne dit rien contre le kernel lui-même.
- Rappels red team respectés : state=4 ≠ preuve du contrat f3best complet (ici: BO/ordre/bytes vérifiés) ; preuve refaite à l'exécution (pas héritée) ; B pas commencé avant gel de A.
- Aucune transposition directe Linux→Windows (MCDM/chemins DMA Windows = stack distincte).

---
*Généré par session Buffy/Codebuff 08/09/2026 — exécution A1+A2 conformes à la gouvernance ACCEPT.*
